#include <cstdio>
#include <cstring>
#include <algorithm>

using namespace std;

int main()
{
	freopen ("country.in", "r", stdin);
	freopen ("country.out", "w", stdout);
	int n, m, p, c, t;
	scanf ("%d%d%d%d%d", &n, &m, &p, &c, &t);
	for (int i = 1; i <= t; i++)
		printf("-1\n");
}