#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
int MAXN = 2005;
int a[MAXN];
double DP[MAXN][MAXN];
int main()
{
    int b;
    int n = 0;
    while (scanf("%d", &b) != EOF)
        a[++n] = b
    DP[]
    for (int i = 1; i <= n; i++)
    {
        for (int j = 1; j <= n; j++)
    }
}