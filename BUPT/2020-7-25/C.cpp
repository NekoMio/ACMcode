#include <bits/stdc++.h>
using namespace std;
const int MAXN = 1e6 + 5;
int a[MAXN]
int main() {
  int T;
  scanf ("%d", &T);
  while (T--) {
    int n, m, k;
    scanf ("%d%d%d", &n, &m, &k);
    if(n > m) swap(n, m);
    for (int i = 1; i <= n; i++) 
  }
}
